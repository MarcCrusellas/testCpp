#ifndef GeneralCalc_H
#define GeneralCalc_H

#include <vector>
#include <sstream>
#include <iostream>
#include <string>
#include <cstdarg>

using namespace std;

int sum(int n, ...);
std::vector<std::string> split(std::string s, std::string delimiter);


#endif